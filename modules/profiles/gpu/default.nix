# GPU driver profiles
# This directory contains GPU-specific driver configurations
# Use one of: amd, intel, or nvidia
{
  # Re-export GPU profiles for easy importing
  # Consumers should import the specific GPU profile they need
}

